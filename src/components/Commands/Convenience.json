{
  "data": [
    {
      "command": "ping",
      "required": [],
      "optional": [],
      "description": "Get bot response time in milliseconds."
    },
    {
    "command": "clear|purge|sanitize",
    "required": [],
    "optional": ["limit"],
    "description": "Delete the specified amount of messages in the channel. The command itself is excluded from the count. Defaults to 1 if limit is not specified."
    }
  ]
}